apiVersion: v1
kind: Service
metadata:
  name: {{ include "multi-wordpress.fullname" . }}
  labels:
    {{- include "multi-wordpress.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "multi-wordpress.selectorLabels" . | nindent 4 }}
